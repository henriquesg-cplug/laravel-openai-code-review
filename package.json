{
  "name": "laravel-openai-code-review",
  "version": "1.0.0",
  "description": "GitHub Action para code review de Laravel/PHP usando OpenAI",
  "main": "index.js",
  "scripts": {
    "build": "ncc build index.js -o dist --source-map",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [
    "github-action",
    "code-review",
    "laravel",
    "php",
    "openai"
  ],
  "author": "",
  "license": "MIT",
  "dependencies": {
    "@actions/core": "^1.10.0",
    "@actions/github": "^5.1.1",
    "openai": "^4.0.0"
  },
  "devDependencies": {
    "@vercel/ncc": "^0.36.1"
  }
}
